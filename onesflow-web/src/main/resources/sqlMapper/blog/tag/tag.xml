<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thenorthw.onesflow.common.dao.blog.tag.BlogTagDao">

    <resultMap id="BlogTagMap" type="BlogTag">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="uid" property="uid" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="en" property="en" jdbcType="VARCHAR"/>
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP"/>
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="fullColumns">
        id,
        uid,
        name,
        en,
        gmt_create,
        gmt_modified
    </sql>


    <select id="getAllTag"  parameterType="long" resultMap="BlogTagMap" >
        SELECT
        id,
        uid,
        name,
        en
        FROM blog_tag
        WHERE uid = #{uid};
    </select>

    <select id="getTagByName"  resultMap="BlogTagMap">
        SELECT
        id,
        uid,
        name,
        en
        FROM blog_tag
        WHERE uid = #{uid} AND name = #{name};
    </select>

    <select id="getTagByEn" resultMap="BlogTagMap">
        SELECT
        id,
        uid,
        name,
        en
        FROM blog_tag
        WHERE uid = #{uid} AND en = #{en};
    </select>


    <select id="getTagById" resultMap="BlogTagMap">
        SELECT
        id,
        uid,
        name,
        en
        FROM blog_tag
        WHERE uid = #{uid} AND id = #{gid};
    </select>


    <insert id="addTag" parameterType="BlogTag" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO blog_tag(
        uid,
        name,
        en,
        gmt_create,
        gmt_modified
        )
        VALUES(#{uid},#{name},#{en},now(),now())
    </insert>

    <update id="updateTag" parameterType="BlogTag">
        UPDATE blog_tag
        SET name = #{name}, en = #{en},gmt_modified = now()
        WHERE id = #{id}
    </update>

    <delete id="deleteTag" parameterType="Long">
        DELETE FROM blog_tag
        WHERE id = #{id}
    </delete>
</mapper>